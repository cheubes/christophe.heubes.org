---
layout: null
permalink: /sitemap.xml
sitemap:
  exclude: 'yes'
---
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
  xmlns:image="http://www.google.com/schemas/sitemap-image/1.1"
  xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {%- for page in site.pages -%}
    {%- if page.title -%}
      {%- if page.lang == "fr" -%}
        {%- unless page.sitemap.exclude == "yes" %}
          <url>
            <loc>{{ site.url }}{{ page.url }}</loc>
            {% if page.sitemap.lastmod -%}
              <lastmod>{{ page.sitemap.lastmod | date: "%Y-%m-%d" }}</lastmod>
            {%- elsif page.date -%}
              <lastmod>{{ page.date | date_to_xmlschema }}</lastmod>
            {%- else -%}
              <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
            {%- endif -%}
            {%- if page.ref == "technical-drawing" -%}
              {%- for item in site.data.technical-drawing %}
            <image:image><image:loc>{{ site.url }}/technical-drawing/{{ item.id }}-2020.jpg</image:loc></image:image>
              {%- endfor -%}
            {%- endif -%}
            {%- assign alternates=site.pages | where:"ref", page.ref | sort: 'lang' -%}
            {%- for alternate in alternates reversed %}
            <xhtml:link rel="alternate" hreflang="{{alternate.lang}}" href="{{site.url }}{{alternate.url}}" />
            {%- endfor %}
          </url>
        {%- endunless -%}
      {%- endif -%}
    {%- endif -%}
  {%- endfor %}
</urlset>
